appId: com.household.shared.shared_household_account
---
# C02: 로그인 성공 (user2)
# user2 계정으로 로그인 후 캘린더 화면이 표시되는지 확인

- runFlow:
    file: '../../flows/common/setup.yaml'
    env:
      EMAIL: 'user2@test.com'
      PASSWORD: 'testpass123'

# 캘린더 화면 진입 확인
- assertVisible: '지출'

- takeScreenshot: 'screenshots/c02'
