# 결제수단 관리 + 자동수집내역 통합 디자인 시안

**생성일:** 2026-01-22
**목적:** 멤버별 결제수단 관리 + 자동수집내역(SMS/Push) 통합

---

## 📋 요구사항 분석

### 현재 구조 (이미지 기준)
```
결제수단 관리 페이지
├─ 상단 우측: 🔔 종 모양 아이콘 (알림)
├─ 탭 1: "결제수단 (SMS)"
├─ 탭 2: "거래 내역"
└─ 카드 리스트:
   ├─ KB국민카드 (수정/삭제)
   ├─ 자동 서리 제안 (🔔 제안)
   └─ 현금 (수정/삭제)
```

### 변경 요구사항
1. ✅ **멤버별 탭 추가**: "나" / "파트너" 탭으로 결제수단 구분
2. ✅ **탭 이름 변경**: "거래 내역" → "자동수집내역"
3. ✅ **종 모양 기능 이동**: 우측 상단 종 → "자동수집내역" 탭에 통합
4. ✅ **자동수집내역 하위 탭**: 대기중 / 확인됨 / 거부됨
5. ✅ **뱃지 알림**: "자동수집내역" 탭에 미확인 개수 표시
6. ✅ **알림 제거**: 탭 선택 시 viewed 처리

---

## 🎨 디자인 시안

---

## 시안 1: 2레벨 탭 구조 (추천 ⭐⭐⭐⭐⭐)

### 레이아웃
```
┌─────────────────────────────────────────┐
│ ← 결제수단 관리                    ⋮    │
├─────────────────────────────────────────┤
│                                         │
│  ┌──────────┬───────────┬──────────┐   │
│  │ 나(김철수) │   파트너   │ 자동수집 🔴3 │   │
│  └──────────┴───────────┴──────────┘   │
│   ▔▔▔▔▔▔▔▔                             │
│                                         │
│  💳 내 결제수단                          │
│                                         │
│  ┌─────────────────────────────────┐   │
│  │ K  KB국민카드                    │   │
│  │    ✏️ 수정  🗑️ 삭제              │   │
│  └─────────────────────────────────┘   │
│                                         │
│  ┌─────────────────────────────────┐   │
│  │ 현  현금                         │   │
│  │    ✏️ 수정  🗑️ 삭제              │   │
│  └─────────────────────────────────┘   │
│                                         │
│  [+ 결제수단 추가]                      │
│                                         │
└─────────────────────────────────────────┘

"자동수집" 탭 선택 시:
┌─────────────────────────────────────────┐
│ ← 결제수단 관리                    ⋮    │
├─────────────────────────────────────────┤
│                                         │
│  ┌──────────┬───────────┬──────────┐   │
│  │    나    │   파트너   │ 자동수집   │   │
│  └──────────┴───────────┴──────────┘   │
│                            ▔▔▔▔▔▔▔    │
│                                         │
│  ┌─────────┬──────────┬──────────┐     │
│  │ 대기중 3 │  확인됨   │  거부됨   │     │
│  └─────────┴──────────┴──────────┘     │
│   ▔▔▔▔▔▔▔                             │
│                                         │
│  📱 대기 중인 거래 (3건)                 │
│                                         │
│  ┌─────────────────────────────────┐   │
│  │ KB국민카드                       │   │
│  │ 스타벅스 - 5,500원               │   │
│  │ 2026.01.22 14:30                │   │
│  │  [✓ 확인]  [✗ 거부]             │   │
│  └─────────────────────────────────┘   │
│                                         │
│  ┌─────────────────────────────────┐   │
│  │ 토스                            │   │
│  │ 쿠팡 - 12,000원                 │   │
│  │ 2026.01.22 10:15                │   │
│  │  [✓ 확인]  [✗ 거부]             │   │
│  └─────────────────────────────────┘   │
│                                         │
│  ┌─────────────────────────────────┐   │
│  │ 현금                            │   │
│  │ 편의점 - 3,200원                │   │
│  │ 2026.01.21 22:45                │   │
│  │  [✓ 확인]  [✗ 거부]             │   │
│  └─────────────────────────────────┘   │
│                                         │
└─────────────────────────────────────────┘
```

### 장점
- ✅ **명확한 구조**: 1레벨(멤버/자동수집), 2레벨(대기/확인/거부)
- ✅ **일관성**: 기존 탭 패턴 재사용
- ✅ **뱃지 가시성**: "자동수집" 탭에 빨간 뱃지로 미확인 개수 표시
- ✅ **컨텍스트 유지**: 같은 페이지 내에서 모든 기능 접근

### 단점
- ⚠️ 탭이 3개 이상이면 좁아질 수 있음

### 구현 코드
```dart
class PaymentMethodManagementPage extends ConsumerStatefulWidget {
  @override
  ConsumerState<PaymentMethodManagementPage> createState() => _State();
}

class _State extends ConsumerState<PaymentMethodManagementPage>
    with TickerProviderStateMixin {
  late TabController _mainTabController;
  late TabController _autoCollectTabController;

  @override
  void initState() {
    super.initState();
    final members = ref.read(ledgerMembersProvider);
    // 멤버 수 + 자동수집 1개
    _mainTabController = TabController(
      length: members.length + 1,
      vsync: this,
    );
    _autoCollectTabController = TabController(
      length: 3, // 대기중, 확인됨, 거부됨
      vsync: this,
    );

    // 자동수집 탭 선택 시 viewed 처리
    _mainTabController.addListener(() {
      if (_mainTabController.index == members.length) {
        _markAsViewed();
      }
    });
  }

  Future<void> _markAsViewed() async {
    await ref.read(pendingTransactionNotifierProvider.notifier)
        .markAllAsViewed();
  }

  @override
  Widget build(BuildContext context) {
    final currentUser = ref.watch(currentUserProvider);
    final members = ref.watch(ledgerMembersProvider);
    final pendingCount = ref.watch(pendingTransactionCountProvider);

    return Scaffold(
      appBar: AppBar(
        title: Text('결제수단 관리'),
        bottom: TabBar(
          controller: _mainTabController,
          tabs: [
            // 멤버별 탭
            ...members.map((member) {
              final isMe = member.userId == currentUser?.id;
              return Tab(
                text: isMe ? '나 (${member.nickname})' : member.nickname,
              );
            }),
            // 자동수집 탭 (뱃지 포함)
            Tab(
              child: Row(
                mainAxisSize: MainAxisSize.min,
                children: [
                  Text('자동수집'),
                  if (pendingCount.value ?? 0 > 0) ...[
                    SizedBox(width: Spacing.xs),
                    Container(
                      padding: EdgeInsets.symmetric(
                        horizontal: 6,
                        vertical: 2,
                      ),
                      decoration: BoxDecoration(
                        color: Theme.of(context).colorScheme.error,
                        borderRadius: BorderRadius.circular(10),
                      ),
                      child: Text(
                        '${pendingCount.value}',
                        style: TextStyle(
                          color: Colors.white,
                          fontSize: 11,
                          fontWeight: FontWeight.bold,
                        ),
                      ),
                    ),
                  ],
                ],
              ),
            ),
          ],
        ),
      ),
      body: TabBarView(
        controller: _mainTabController,
        children: [
          // 멤버별 결제수단 뷰
          ...members.map((member) {
            return PaymentMethodListView(
              memberId: member.userId,
              isOwner: member.userId == currentUser?.id,
            );
          }),
          // 자동수집 뷰
          Column(
            children: [
              TabBar(
                controller: _autoCollectTabController,
                tabs: [
                  Tab(text: '대기중'),
                  Tab(text: '확인됨'),
                  Tab(text: '거부됨'),
                ],
              ),
              Expanded(
                child: TabBarView(
                  controller: _autoCollectTabController,
                  children: [
                    PendingTransactionListView(
                      status: PendingTransactionStatus.pending,
                    ),
                    PendingTransactionListView(
                      status: PendingTransactionStatus.converted,
                    ),
                    PendingTransactionListView(
                      status: PendingTransactionStatus.rejected,
                    ),
                  ],
                ),
              ),
            ],
          ),
        ],
      ),
    );
  }
}
```

---

## 시안 2: 플로팅 액션 버튼 방식 (추천 ⭐⭐⭐⭐)

### 레이아웃
```
┌─────────────────────────────────────────┐
│ ← 결제수단 관리                    ⋮    │
├─────────────────────────────────────────┤
│                                         │
│  ┌──────────┬───────────┐              │
│  │ 나(김철수) │   파트너   │              │
│  └──────────┴───────────┘              │
│   ▔▔▔▔▔▔▔▔                             │
│                                         │
│  💳 내 결제수단                          │
│                                         │
│  ┌─────────────────────────────────┐   │
│  │ K  KB국민카드                    │   │
│  │    ✏️ 수정  🗑️ 삭제              │   │
│  └─────────────────────────────────┘   │
│                                         │
│  ┌─────────────────────────────────┐   │
│  │ 현  현금                         │   │
│  │    ✏️ 수정  🗑️ 삭제              │   │
│  └─────────────────────────────────┘   │
│                                         │
│  [+ 결제수단 추가]                      │
│                                         │
│                            ┌──────┐    │
│                            │ 📱 3  │    │ <- FAB
│                            └──────┘    │
└─────────────────────────────────────────┘

FAB 클릭 시 바텀시트:
┌─────────────────────────────────────────┐
│                                         │
│  ──────────────────────────────────    │
│                                         │
│  📱 자동수집내역 (3건)            ✕     │
│                                         │
│  ┌─────────┬──────────┬──────────┐     │
│  │ 대기중 3 │  확인됨   │  거부됨   │     │
│  └─────────┴──────────┴──────────┘     │
│   ▔▔▔▔▔▔▔                             │
│                                         │
│  ┌─────────────────────────────────┐   │
│  │ KB국민카드                       │   │
│  │ 스타벅스 - 5,500원               │   │
│  │  [✓ 확인]  [✗ 거부]             │   │
│  └─────────────────────────────────┘   │
│                                         │
└─────────────────────────────────────────┘
```

### 장점
- ✅ **분리된 컨텍스트**: 결제수단 관리와 자동수집이 독립적
- ✅ **FAB 뱃지 강조**: 빨간 뱃지가 더 눈에 띔
- ✅ **공간 효율**: 상단 탭 공간 절약

### 단점
- ⚠️ 한 번의 추가 액션 필요 (FAB 클릭)
- ⚠️ 바텀시트가 닫히면 컨텍스트 상실

---

## 시안 3: 상단 배너 방식 (추천 ⭐⭐⭐)

### 레이아웃
```
┌─────────────────────────────────────────┐
│ ← 결제수단 관리                    ⋮    │
├─────────────────────────────────────────┤
│                                         │
│  ┌─────────────────────────────────┐   │
│  │ 📱 새로운 거래 3건 발견!         │   │
│  │    자동수집내역 확인하기 →       │   │
│  └─────────────────────────────────┘   │
│                                         │
│  ┌──────────┬───────────┐              │
│  │ 나(김철수) │   파트너   │              │
│  └──────────┴───────────┘              │
│   ▔▔▔▔▔▔▔▔                             │
│                                         │
│  💳 내 결제수단                          │
│                                         │
│  ┌─────────────────────────────────┐   │
│  │ K  KB국민카드                    │   │
│  │    ✏️ 수정  🗑️ 삭제              │   │
│  └─────────────────────────────────┘   │
│                                         │
└─────────────────────────────────────────┘

배너 클릭 시:
→ 자동수집내역 전체 화면으로 이동
```

### 장점
- ✅ **주목도 높음**: 상단 배너로 즉시 눈에 띔
- ✅ **액션 유도**: "확인하기" CTA 버튼
- ✅ **미확인 시에만 표시**: 확인 후 배너 사라짐

### 단점
- ⚠️ 화면 공간 차지
- ⚠️ 별도 페이지로 이동 필요

---

## 시안 4: 우측 드로어 방식 (추천 ⭐⭐⭐)

### 레이아웃
```
┌─────────────────────────────────────────┐
│ ← 결제수단 관리              📱3    ⋮   │ <- 우측 아이콘
├─────────────────────────────────────────┤
│                                         │
│  ┌──────────┬───────────┐              │
│  │ 나(김철수) │   파트너   │              │
│  └──────────┴───────────┘              │
│   ▔▔▔▔▔▔▔▔                             │
│                                         │
│  💳 내 결제수단                          │
│                                         │
└─────────────────────────────────────────┘

우측 아이콘 클릭 시 드로어:
┌──────────────────┬──────────────────────┐
│                  │ 📱 자동수집내역    ✕  │
│                  ├──────────────────────┤
│                  │                      │
│                  │ ┌─────┬─────┬─────┐ │
│  (메인 화면)      │ │대기중│확인됨│거부됨│ │
│                  │ └─────┴─────┴─────┘ │
│                  │  ▔▔▔▔▔              │
│                  │                      │
│                  │ ┌──────────────────┐ │
│                  │ │ KB국민카드        │ │
│                  │ │ 스타벅스 - 5,500 │ │
│                  │ │ [✓]  [✗]        │ │
│                  │ └──────────────────┘ │
│                  │                      │
└──────────────────┴──────────────────────┘
```

### 장점
- ✅ **멀티태스킹**: 결제수단 보면서 자동수집 처리 가능
- ✅ **빠른 접근**: 우측 아이콘 클릭만으로 즉시 열람

### 단점
- ⚠️ 모바일에서 드로어 너비 제한
- ⚠️ iOS/Android 네이티브 패턴과 다름

---

## 시안 5: 스와이프 제스처 (추천 ⭐⭐)

### 레이아웃
```
┌─────────────────────────────────────────┐
│ ← 결제수단 관리                    ⋮    │
├─────────────────────────────────────────┤
│                                         │
│  ┌──────────┬───────────┐  📱3         │
│  │ 나(김철수) │   파트너   │  ←─ 스와이프 │
│  └──────────┴───────────┘              │
│   ▔▔▔▔▔▔▔▔                             │
│                                         │
│  💳 내 결제수단                          │
│                                         │
└─────────────────────────────────────────┘

좌측 스와이프:
┌─────────────────────────────────────────┐
│ ← 결제수단 관리                    ⋮    │
├─────────────────────────────────────────┤
│                                         │
│  ┌──────────┬───────────┬──────────┐   │
│  │    나    │   파트너   │ 자동수집 🔴3 │
│  └──────────┴───────────┴──────────┘   │
│                            ▔▔▔▔▔▔▔    │
│  ┌─────────┬──────────┬──────────┐     │
│  │ 대기중 3 │  확인됨   │  거부됨   │     │
│  └─────────┴──────────┴──────────┘     │
└─────────────────────────────────────────┘
```

### 장점
- ✅ **제스처 기반**: 빠른 전환

### 단점
- ⚠️ 발견 가능성 낮음 (Hidden feature)
- ⚠️ 학습 곡선 필요

---

## 📊 시안 비교표

| 항목 | 시안 1 (2레벨 탭) | 시안 2 (FAB) | 시안 3 (배너) | 시안 4 (드로어) | 시안 5 (스와이프) |
|------|------------------|-------------|-------------|----------------|------------------|
| **뱃지 가시성** | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐ | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐ | ⭐⭐⭐ |
| **접근 편의성** | ⭐⭐⭐⭐⭐ | ⭐⭐⭐ | ⭐⭐⭐⭐ | ⭐⭐⭐⭐ | ⭐⭐ |
| **공간 효율** | ⭐⭐⭐ | ⭐⭐⭐⭐⭐ | ⭐⭐⭐ | ⭐⭐⭐⭐ | ⭐⭐⭐⭐⭐ |
| **일관성** | ⭐⭐⭐⭐⭐ | ⭐⭐⭐ | ⭐⭐⭐⭐ | ⭐⭐⭐ | ⭐⭐ |
| **구현 복잡도** | ⭐⭐⭐⭐ | ⭐⭐⭐ | ⭐⭐⭐⭐⭐ | ⭐⭐⭐ | ⭐⭐ |
| **확장성** | ⭐⭐⭐ | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐ | ⭐⭐⭐ |

---

## 🎯 최종 추천

### 1순위: 시안 1 (2레벨 탭 구조) ⭐⭐⭐⭐⭐

**추천 이유:**
1. ✅ **기존 패턴 활용**: 이미 탭을 사용 중 (SMS/거래내역)
2. ✅ **명확한 계층**: 멤버 → 결제수단, 자동수집 → 상태
3. ✅ **뱃지 강조**: "자동수집" 탭에 빨간 뱃지로 주목도 극대화
4. ✅ **컨텍스트 유지**: 같은 화면에서 모든 기능 접근
5. ✅ **학습 불필요**: 직관적인 탭 네비게이션

**개선 제안:**
- 탭이 좁아질 경우 → ScrollableTabBar 적용
- 뱃지 애니메이션 추가 (펄스 효과)

### 2순위: 시안 2 (FAB) ⭐⭐⭐⭐

**추천 이유:**
- ✅ 결제수단과 자동수집이 독립적인 기능
- ✅ FAB이 항상 보이므로 접근성 좋음
- ✅ 공간 효율적

**적합한 경우:**
- 자동수집을 별도 기능으로 강조하고 싶을 때

---

## 🎨 디자인 세부사항

### 1. 뱃지 디자인

```dart
// 1. 탭 텍스트 옆 뱃지
Container(
  padding: EdgeInsets.symmetric(horizontal: 6, vertical: 2),
  decoration: BoxDecoration(
    color: Theme.of(context).colorScheme.error,
    borderRadius: BorderRadius.circular(10),
  ),
  child: Text(
    '$count',
    style: TextStyle(
      color: Colors.white,
      fontSize: 11,
      fontWeight: FontWeight.bold,
    ),
  ),
)

// 2. 아이콘 위 뱃지 (상대 위치)
Badge(
  label: Text('$count'),
  backgroundColor: Theme.of(context).colorScheme.error,
  child: Icon(Icons.receipt_long),
)

// 3. 펄스 애니메이션 뱃지
AnimatedContainer(
  duration: Duration(milliseconds: 500),
  decoration: BoxDecoration(
    color: Colors.red,
    shape: BoxShape.circle,
    boxShadow: [
      BoxShadow(
        color: Colors.red.withValues(alpha: 0.5),
        blurRadius: _isAnimating ? 10 : 0,
        spreadRadius: _isAnimating ? 3 : 0,
      ),
    ],
  ),
  child: Text('$count'),
)
```

### 2. 탭 스타일

```dart
TabBar(
  indicatorWeight: 3,
  indicatorColor: Theme.of(context).colorScheme.primary,
  labelColor: Theme.of(context).colorScheme.primary,
  unselectedLabelColor: Theme.of(context).colorScheme.onSurface.withValues(alpha: 0.6),
  labelStyle: TextStyle(
    fontWeight: FontWeight.bold,
    fontSize: 14,
  ),
  unselectedLabelStyle: TextStyle(
    fontWeight: FontWeight.normal,
    fontSize: 14,
  ),
  tabs: [...],
)
```

### 3. 자동수집 리스트 아이템

```dart
class PendingTransactionCard extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return AppCard(
      child: Column(
        crossAxisAlignment: CrossAxisAlignment.start,
        children: [
          // 결제수단
          Row(
            children: [
              CircleAvatar(
                backgroundColor: transaction.paymentMethod.color,
                child: Text(transaction.paymentMethod.initial),
              ),
              SizedBox(width: Spacing.sm),
              Text(
                transaction.paymentMethod.name,
                style: Theme.of(context).textTheme.titleMedium,
              ),
            ],
          ),
          SizedBox(height: Spacing.xs),
          // 가맹점 & 금액
          Text(
            transaction.parsedMerchant ?? '미확인',
            style: Theme.of(context).textTheme.bodyLarge,
          ),
          Text(
            '${NumberFormat('#,###').format(transaction.parsedAmount)}원',
            style: Theme.of(context).textTheme.headlineSmall?.copyWith(
              color: transaction.parsedType == 'expense'
                  ? Theme.of(context).colorScheme.error
                  : Theme.of(context).colorScheme.primary,
              fontWeight: FontWeight.bold,
            ),
          ),
          SizedBox(height: Spacing.xs),
          // 날짜
          Text(
            DateFormat('yyyy.MM.dd HH:mm').format(transaction.parsedDate),
            style: Theme.of(context).textTheme.bodySmall?.copyWith(
              color: Theme.of(context).colorScheme.onSurface.withValues(alpha: 0.6),
            ),
          ),
          SizedBox(height: Spacing.md),
          // 액션 버튼
          Row(
            mainAxisAlignment: MainAxisAlignment.end,
            children: [
              OutlinedButton.icon(
                icon: Icon(Icons.close, size: IconSize.sm),
                label: Text('거부'),
                onPressed: () => _onReject(context),
              ),
              SizedBox(width: Spacing.sm),
              ElevatedButton.icon(
                icon: Icon(Icons.check, size: IconSize.sm),
                label: Text('확인'),
                onPressed: () => _onConfirm(context),
              ),
            ],
          ),
        ],
      ),
    );
  }
}
```

---

## 💡 추가 제안

### 제안 1: 스마트 알림 감소

**문제:** 매번 SMS가 올 때마다 뱃지 증가 → 피로도 상승

**해결:**
```dart
// 같은 결제수단의 거래가 5분 이내 여러 건이면 1개로 그룹핑
final groupedTransactions = _groupByPaymentMethodAndTime(
  transactions,
  duration: Duration(minutes: 5),
);

// 뱃지 개수 = 그룹 개수
final badgeCount = groupedTransactions.length;
```

### 제안 2: 자동 확인 기능

**문제:** 자주 가는 곳은 매번 확인하기 번거로움

**해결:**
```dart
// 학습된 패턴 자동 확인 (사용자 동의 필요)
if (transaction.matchesLearnedPattern()) {
  await _autoConfirm(transaction);
  _showSnackBar('자주 가는 가맹점이라 자동으로 확인했습니다');
}
```

### 제안 3: 빠른 액션 (스와이프)

**문제:** 카드 → 버튼 클릭 2단계 필요

**해결:**
```dart
Dismissible(
  key: Key(transaction.id),
  direction: DismissDirection.horizontal,
  confirmDismiss: (direction) async {
    if (direction == DismissDirection.startToEnd) {
      // 우측 스와이프 → 확인
      await _onConfirm(transaction);
      return true;
    } else {
      // 좌측 스와이프 → 거부
      await _onReject(transaction);
      return true;
    }
  },
  background: Container(
    color: Colors.green,
    child: Icon(Icons.check, color: Colors.white),
  ),
  secondaryBackground: Container(
    color: Colors.red,
    child: Icon(Icons.close, color: Colors.white),
  ),
  child: PendingTransactionCard(transaction: transaction),
)
```

### 제안 4: 음성 피드백 (선택적)

**문제:** 화면을 보지 않고도 상태 확인하고 싶음

**해결:**
```dart
// 자동수집 탭 진입 시
TTS.speak('${pendingCount}건의 대기 중인 거래가 있습니다');

// 확인/거부 시
TTS.speak('거래가 확인되었습니다'); // or '거래가 거부되었습니다'
```

---

## 🔄 기존 기능 변경 사항

### 1. 종 모양 아이콘 제거

```dart
// Before (기존)
AppBar(
  title: Text('결제수단 관리'),
  actions: [
    IconButton(
      icon: Badge(
        label: Text('$pendingCount'),
        child: Icon(Icons.notifications),
      ),
      onPressed: () {
        Navigator.push(context, PendingTransactionPage());
      },
    ),
  ],
)

// After (변경 후)
AppBar(
  title: Text('결제수단 관리'),
  // actions 제거
)
```

### 2. 탭 이름 변경

```dart
// Before
TabBar(
  tabs: [
    Tab(text: '결제수단 (SMS)'),
    Tab(text: '거래 내역'),
  ],
)

// After
TabBar(
  tabs: [
    Tab(text: '나 (김철수)'),
    Tab(text: '파트너'),
    Tab(
      child: Row(
        children: [
          Text('자동수집'),
          Badge(label: Text('$count')),
        ],
      ),
    ),
  ],
)
```

### 3. viewed 처리 로직

```dart
// TabController 리스너 추가
_mainTabController.addListener(() {
  if (_mainTabController.index == _autoCollectTabIndex) {
    // 자동수집 탭 선택 시 viewed 처리
    ref.read(pendingTransactionNotifierProvider.notifier)
        .markAllAsViewed();
  }
});
```

---

## 📱 화면 플로우

```
결제수단 관리 페이지
│
├─ [나] 탭
│  ├─ 내 결제수단 리스트
│  ├─ [+ 결제수단 추가] 버튼
│  └─ 수정/삭제 가능
│
├─ [파트너] 탭
│  ├─ 파트너 결제수단 리스트
│  └─ 조회만 가능 (수정/삭제 불가)
│
└─ [자동수집 🔴3] 탭
   ├─ [대기중 3] 탭
   │  ├─ 대기 중인 거래 리스트
   │  └─ [✓ 확인] [✗ 거부] 버튼
   │
   ├─ [확인됨] 탭
   │  └─ 확인된 거래 리스트 (이미 거래로 전환됨)
   │
   └─ [거부됨] 탭
      └─ 거부된 거래 리스트
      └─ [모두 삭제] 버튼
```

---

## ❓ 사용자 확인 질문

### Q1: 시안 1 (2레벨 탭)이 맞나요?
- ✅ 맞다면: 바로 구현 시작
- ❌ 다르다면: 다른 시안 선택 또는 추가 요구사항 제시

### Q2: 뱃지 위치는 어디가 좋을까요?
- A. "자동수집" 탭 텍스트 오른쪽 (시안 1)
- B. "자동수집" 아이콘 위에 작은 점
- C. 우측 상단 FAB (시안 2)

### Q3: 자동수집 탭 진입 시 모든 알림을 제거하나요?
- A. ✅ 진입 즉시 모두 제거 (권장)
- B. "대기중" 탭 선택 시에만 제거
- C. 각 거래를 확인/거부해야만 제거

### Q4: 추가 기능이 필요한가요?
- A. 스와이프로 빠른 확인/거부 (제안 3)
- B. 자주 가는 곳 자동 확인 (제안 2)
- C. 현재 기능만으로 충분

---

## 🎯 최종 디자인 결정

**사용자 선택을 기다리고 있습니다:**

1. **시안 선택**: 1 / 2 / 3 / 4 / 5 또는 혼합
2. **뱃지 위치**: A / B / C
3. **알림 제거 시점**: A / B / C
4. **추가 기능**: A / B / C / 없음

선택하시면 즉시 구현을 시작하겠습니다! 🚀
